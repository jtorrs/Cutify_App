rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Barber profile photos — public read so customer apps can show images; write requires signed-in user.
    match /barber_photos/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
